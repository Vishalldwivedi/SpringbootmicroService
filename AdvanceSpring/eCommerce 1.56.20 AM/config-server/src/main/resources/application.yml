spring.application.name: config-server
spring:
  cloud:
    config:
      server:
        git:
          uri:
          username:
          password:
          default-label: master
server:
  port: 8888




